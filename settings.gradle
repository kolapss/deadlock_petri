rootProject.name = 'deadlock_petri'

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
        maven{ url 'https://mvnrepository.com/artifact'}
        maven { url 'https://jitpack.io' }
    }
}